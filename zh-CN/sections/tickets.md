# 工单

工单是Escalated的核心。每个工单都是一个具有完整对话历史、优先级级别、分配跟踪和SLA监控的长期状态机。

> **注意：** 每个工单都有一个唯一引用号（例如`ESC-00001`），支持自定义状态、优先级级别、部门分配、标签、SLA跟踪以及带附件的丰富对话。所有数据存储在您的数据库中。

## 高级工单工作流

- 工单合并流程，在保留历史记录的同时合并重复工单
- 用于关联事件/问题工作流的工单关联
- 用于并行内部/外部协调的侧边对话
- 用于受限运营角色的轻量客服支持
- 基于工单上下文的条件规则自定义字段表单
