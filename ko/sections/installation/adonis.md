## 1. 패키지 설치

```bash
$ npm install @escalated-dev/escalated-adonis
```

## 2. 패키지 설정

```bash
$ node ace configure @escalated-dev/escalated-adonis
```

## 3. 마이그레이션 실행

```bash
$ node ace migration:run
```

> **참고:** configure 명령은 `config/escalated.ts` 퍼블리싱, 프로바이더 등록, 마이그레이션 복사를 자동으로 수행합니다.
