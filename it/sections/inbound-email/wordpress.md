### Configurazione

Configura le email in entrata sotto **Escalated -> Settings -> Email** in wp-admin. Supporta i webhook di Mailgun, Postmark e AWS SES.

### Polling IMAP

Il polling IMAP viene eseguito tramite WP-Cron. Il plugin lo pianifica automaticamente quando IMAP è configurato come adattatore in entrata.
