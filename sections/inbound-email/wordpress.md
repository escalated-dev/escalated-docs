### Configuration

Configure inbound email under **Escalated -> Settings -> Email** in wp-admin. Supports Mailgun, Postmark, and AWS SES webhooks.

### IMAP Polling

IMAP polling runs via WP-Cron. The plugin schedules it automatically when IMAP is configured as the inbound adapter.
