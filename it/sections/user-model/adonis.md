```typescript
// The AdonisJS package uses your configured userModel
// from config/escalated.ts — no trait needed.
// Users are linked via the foreign key on the tickets table.
```

Configura il percorso di importazione del modello utente in `config/escalated.ts` sotto `userModel`.
