### Configuracao

Configure o e-mail de entrada em **Escalated -> Settings -> Email** no wp-admin. Suporta webhooks de Mailgun, Postmark e AWS SES.

### Polling IMAP

O polling IMAP e executado via WP-Cron. O plugin o agenda automaticamente quando o IMAP e configurado como o adaptador de entrada.
